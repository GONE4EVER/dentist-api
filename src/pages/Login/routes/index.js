import Login from 'pages/Login/Login.vue';

import { icon, name, path } from 'pages/Login/routes/constants';


const routes = {
  icon,
  path,
  name,
  component: Login,
};


export default routes;
